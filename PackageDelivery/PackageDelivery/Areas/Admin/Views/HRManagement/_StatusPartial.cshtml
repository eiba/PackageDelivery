@model PackageDelivery.Areas.Admin.Models.StatusModel
@*Deactivate/activate button*@
@Ajax.ActionLink(Model.status,
    Model.action,
    "HRManagement",
    new { id = Model.Id },
    new AjaxOptions
    {
        UpdateTargetId = Model.Id,
        InsertionMode = InsertionMode.Replace,
        HttpMethod = "GET"
    },new {@class= "btn btn-danger j bold" })